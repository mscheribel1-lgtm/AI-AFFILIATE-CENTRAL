// This layout file is required by Next.js to ensure the /admin route
// correctly inherits the main root layout (app/layout.js).
// It simply acts as a passthrough component.

export default function AdminLayout({ children }) {
  return (
    // The children here will be app/admin/page.js
    <>{children}</>
  );
}

**Instructions Summary:**

1.  **Go to GitHub** to the **`app`** folder.
2.  Click **"Add file"** $\rightarrow$ **"Create new file"**.
3.  In the file name box, type the full path: **`admin/layout.js`**
4.  Paste the code above.
5.  **Commit** the file.

Please confirm once you have committed the file!
